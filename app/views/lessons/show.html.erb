<h1><%= @lesson.name %></h1>
<div>
  <%= @lesson.content %>
</div>

<%= form_tag(lesson_check_path(@lesson.id), method: "post") do %>
  <div>
    <%= text_field_tag(:answer) %>
  </div>
  <div>
    <%= submit_tag "Check" %>
<% end %>